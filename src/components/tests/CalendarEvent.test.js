import React from 'react'
import { shallow } from 'enzyme'
import { expect } from 'chai'
import { CalendarEvent } from '../CalendarEvent'

const testEvent = {}

const mockOpenPopper = jest.fn()

describe('<CalendarEvent />', () => {
  it('default case', () => {})
})
